# swarm_project
 Servei web d’alta disponibilitat amb Nginx,   securitzat i accés encriptat , amb tecnología  de containers. 

 docker run -it --rm -v "C:\Ruta del fitxer" -v //var/run/docker.sock:/var/run/docker.sock -w /work toolbox
 
sed -i 's/\r$//' *.sh

chmod +x *.sh

./deploy.sh
